# Stubs

Contains pre-compiled binaries used by benchmark scripts. Only the JAR
`no-limit-stub.jar` is committed; source code is never exposed in this
public repository. The stub simply prints JSON with realistic compression
metrics.